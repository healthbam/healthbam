(function (angular) {
    "use strict";

    angular.module(
        "healthBam.authentication",
        [
            "ngMaterial",
            "satellizer",
            "healthBam.config",
            "healthBam.errorHandling"
        ]
    );

}(window.angular));
