(function (angular) {
    "use strict";

    angular.module(
        "healthBam.authentication",
        [
            "ngMaterial",
            "satellizer",
            "healthBam.errorHandling"
        ]
    );

}(window.angular));
