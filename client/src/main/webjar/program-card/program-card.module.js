(function (angular) {
    "use strict";

    angular.module(
        "healthBam.programCard",
        [
            "ngMaterial",
            "healthBam.templates"
        ]
    );

}(window.angular));
