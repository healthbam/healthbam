(function (angular) {
    "use strict";

    angular.module(
        "healthBam.toolbar",
        [
            "ngMaterial",
            "healthBam.templates",
            "healthBam.authentication"
        ]
    );

}(window.angular));
