(function (angular) {
    "use strict";

    angular.module(
        "healthBam.organizationCard",
        [
            "ngMaterial",
            "healthBam.templates"
        ]
    );

}(window.angular));
