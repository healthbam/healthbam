(function (angular) {
    "use strict";

    angular.module(
        "healthBam.main",
        [
            "ngMaterial",
            "ui.router",
            "ngMap",
            "healthBam.templates"
        ]
    );

}(window.angular));
