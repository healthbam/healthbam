(function (angular) {
    "use strict";

    angular.module(
        "healthBam.organizationForm",
        [
            "ngMaterial",
            "healthBam.config",
            "healthBam.templates"
        ]
    );

}(window.angular));
