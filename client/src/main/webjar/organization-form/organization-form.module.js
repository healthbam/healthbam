(function (angular) {
    "use strict";

    angular.module(
        "healthBam.organizationForm",
        [
            "ngMaterial",
            "healthBam.templates"
        ]
    );

}(window.angular));
