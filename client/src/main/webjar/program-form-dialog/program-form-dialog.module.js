(function (angular) {
    "use strict";

    angular.module(
        "healthBam.programFormDialog",
        [
            "ngMaterial",
            "healthBam.templates",
            "healthBam.requestedProgram"
        ]
    );

}(window.angular));
