(function (angular) {
    "use strict";

    angular.module(
        "healthBam.createUser",
        [
            "ngMaterial",
            "ui.router",
            "healthBam.userFormDialog",
            "healthBam.authentication"
        ]
    );

}(window.angular));
