(function (angular) {
    "use strict";

    angular.module(
        "healthBam.createProgram",
        [
            "ngMaterial",
            "healthBam.programFormDialog"
        ]
    );

}(window.angular));
